# Optional: set for deployment without running `vercel login`.
# Create a token at https://vercel.com/account/tokens
# Copy this file to .env.local and add your token (do not commit .env.local).
# VERCEL_TOKEN=
